// https://discuss.gradle.org/t/place-for-properties-visible-from-both-buildsrc-and-project/18916/9
Properties properties = new Properties()

try (FileInputStream input = new FileInputStream(file('../gradle.properties'))) {
	properties.load(input)
}

for (String key : properties.stringPropertyNames()) {
	ext.set(key, properties.getProperty(key))
}
