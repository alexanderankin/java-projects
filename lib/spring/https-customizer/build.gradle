plugins {
    id 'info.ankin.projects.spring-conventions'
    id 'info.ankin.projects.library-conventions'
}

dependencies {
    api 'org.springframework.boot:spring-boot'
    implementation 'org.springframework.boot:spring-boot-tomcat'
    implementation 'org.springframework.boot:spring-boot-reactor-netty'
    // spring doesn't manage it
    // managed by info.ankin.projects.conventions.gradle
    implementation 'org.bouncycastle:bcpkix-jdk18on'
    implementation 'io.netty:netty-handler'

    itestImplementation 'org.springframework.boot:spring-boot-starter-webflux'
    itestImplementation 'org.springframework.boot:spring-boot-starter-restclient'
    itestImplementation 'org.springframework.boot:spring-boot-starter-webclient'
    itestImplementation 'org.springframework.boot:spring-boot-starter-test'
    itestImplementation 'org.springframework:spring-web'
    itestImplementation 'org.apache.httpcomponents.client5:httpclient5'
}

enableConventionConstraints()
